const WebpackPlugIn = require('html-webpack-plugin');
const WebpackPwaManifest = require('webpack-paw-manigest');
const path = require('path');
const { InjectManifest } = require('workbox-webpack-plugin');

module.exports = () => {
    return {
        mode: 'development',
        entry: {
            app: './src/index.js',
            install: './src/install.js'
        },
        output: {
            path: path.resolve(__dirname, 'dist'),
            filename: '[name].bundle.js'
        },
}

{
    "name": "text-editor",
    "version": "1.0.0",
    "engines:": {
        "node": "14.17.3",
        "npm": "6.14.13"
    },
    "description": "Just another text editor!",
    "main": "server.js",
    "scripts": {
        "server": "nodemon server.js --ignore client"
    },
    "keywords": [],
    "author": "Lauren Pineiro",
    "license": "ISC",
    "dependencies": {
        "express": "^4.17.1"
    },
    "devDependencies": {
        "nodemon": "^2.0.4"
    }
}
